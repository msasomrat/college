#include <stdio.h>
int main()
{
    int a,sum,c=0,x,z;
    scanf("%d", &x);
    while (z<=x)
    {
        scanf("%d", &z);
          
    }
    for(a=x,sum=0; sum<z; a++)
    {
        sum+=a;
        c++;
    }
    printf("%d\n", c);
    return 0;
}